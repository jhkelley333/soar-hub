diff a/src/modules/paf/PafForm.tsx b/src/modules/paf/PafForm.tsx	(rejected hunks)
@@ -432,6 +444,7 @@ function FieldGrid({
           onChange={(v) => onChange(k, v)}
           lists={cfg.lists}
           readOnly={isFieldReadOnly(k, state)}
+          myStores={myStores}
         />
       ))}
     </div>
@@ -478,6 +491,7 @@ function FieldRender({
   onChange,
   lists,
   readOnly,
+  myStores,
 }: {
   fieldKey: string;
   cfg: PafFieldDisplay;
@@ -485,6 +499,7 @@ function FieldRender({
   onChange: (v: string) => void;
   lists: PafConfigDoc["lists"];
   readOnly?: boolean;
+  myStores: MyStore[];
 }) {
   const id = `paf-${fieldKey}`;
   const label = (
